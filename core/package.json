{
  "name": "@mini-ml/core",
  "main": "dist/index.js",
  "version": "0.0.1",
  "scripts": {
    "test": "vitest",
    "generate:parser": "ohm generateBundles --withTypes src/parser/grammar.ohm",
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "devDependencies": {
    "@ohm-js/cli": "^2.0.0",
    "@vitest/ui": "^1.1.3",
    "ohm-js": "^17.1.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  }
}
